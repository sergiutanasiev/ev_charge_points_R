import React, { useState } from 'react';
import Form from '../components/form';

const Stations = () => {    

    React.useEffect(() => {
    }, []);

  return (
    <div>
        <Form />
    </div>
  );
};

export default Stations;